---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: azurecontainerregistries.deployments.acrops.io
spec:
  group: deployments.acrops.io
  names:
    kind: AzureContainerRegistry
    listKind: AzureContainerRegistryList
    plural: azurecontainerregistries
    singular: azurecontainerregistry
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: AzureContainerRegistry is the Schema for the azurecontainerregistries
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureContainerRegistrySpec defines the desired state of AzureContainerRegistry
            properties:
              name:
                type: string
            type: object
          status:
            description: AzureContainerRegistryStatus defines the observed state of
              AzureContainerRegistry
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
